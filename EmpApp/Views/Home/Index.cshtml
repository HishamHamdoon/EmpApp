@*using the viewimports to include the common namespaces*@
@model IEnumerable<Employee>
@{
    ViewData["Title"] = "Home Page";
    //var photoPAth ="~/images"+ Model.
}


    @*<table class="table table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Email</th>
                <th>Department</th>
            </tr>
        </thead>*@
<div class="row">
    <div classs="col-md-4">
        <div class="card-deck">
            @foreach (var item in Model)
            {
                var photoPath = "/images/" + (item.PhotoPath ?? "avatar.webp");

                <div class="card m-3" style="min-width:18rem;max-width:30.5%;">
                    <div class="card-header">
                        <h3 class="h3">
                            @item.Name
                        </h3>
                    </div>
                    <img src="@photoPath" alt="Alternate Text" class="image-class"/>
                    <div class="card-footer text-center">

                        <a asp-controller="home" asp-action="details" asp-route-id="@item.Id" class="btn btn-primary m-1">View</a>

                        <a asp-controller="home" asp-action="edit" asp-route-id="@item.Id" class="btn btn-primary m-1">Edit</a>

                        <a asp-controller="home" asp-action="details" asp-route-id="@item.Id" class="btn btn-danger m-1">Delete</a>
                    </div>
                </div>
                @*@item.Id

                @item.Email
                @item.Department*@
            }
        </div>
    </div>
</div>